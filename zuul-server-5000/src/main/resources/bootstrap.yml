spring:
  application:
    name: zuul-server-5000
  cloud:
    config:
      label: master
      uri: http://localhost:4000
      profile: test
      discovery:
        enabled: true
  profiles:
    active: test,dev