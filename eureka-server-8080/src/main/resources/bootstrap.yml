spring:
  application:
    name: eureka-server-8080
  cloud:
    config:
      label: master
      uri: http://localhost:4000
      profile: test
      discovery:
        enabled: true
      name: config-server-4000
  profiles:
    active: test,dev